# Set to "true" to run with the LLM (requires GPU, PyTorch, Transformers)
# Set to "false" to run locally using the heuristic fallback
USE_LLM=false

# Path or HuggingFace ID of the model to use
CONVEVAL_MODEL="Qwen/Qwen2-7B-Instruct"
